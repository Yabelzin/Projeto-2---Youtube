interface IProps {
    turnMenu: boolean;
    seeChannels: boolean;
};

function History({ turnMenu }: IProps, {seeChannels}: IProps) {
    return (
        <>
            
        </>
    )
};

export default History;