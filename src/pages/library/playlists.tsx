interface IProps {
    turnMenu: boolean;
    seeChannels: boolean;
};

function Playlists({ turnMenu }: IProps, {seeChannels}: IProps) {
    return (
        <>
            
        </>
    )
};

export default Playlists;