interface IProps {
    turnMenu: boolean;
    seeChannels: boolean;
};

function You({ turnMenu }: IProps, {seeChannels}: IProps) {
    return (
        <>
            
        </>
    )
};

export default You;